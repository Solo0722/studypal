export const getClassesDataFromDB = async (userId: string) => {
  return [];
};

export const getNotesDataFromDB = async (userId: string) => {
  return [];
};

export const getExamsDataFromDB = async (userId: string) => {};

export const getTasksDataFromDB = async (userId: string) => {};
