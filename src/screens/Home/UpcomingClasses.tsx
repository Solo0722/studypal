import { StyleSheet, Text, View } from "react-native";
import React from "react";

const UpcomingClasses = () => {
  return (
    <View>
      <Text>UpcomingClasses</Text>
    </View>
  );
};

export default UpcomingClasses;

const styles = StyleSheet.create({});
